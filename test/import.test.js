describe("Import", function () {
  it("should do a dynamic import", function (done) {
    const assert = require("node:assert").strict; //TODO: this is not a dynamic import yet. Maybe could change this to the ES import?
    assert.equal(null, null);
    done();
  });
});
